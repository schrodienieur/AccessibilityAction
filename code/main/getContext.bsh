/*────────────────────────────────────────────────────────────────
This code was primarily generated using AI with minimal human editing.
──────────────────────────────────────────────────────────────── */
import android.content.Context;
import android.hardware.display.DisplayManager;
import android.view.Display;
import android.view.WindowManager;

Context getContext(int overlayType, int displayId) {
	Context myContext;
	if (overlayType == WindowManager.LayoutParams.TYPE_ACCESSIBILITY_OVERLAY) {
		myContext = getService();
	} else {
		myContext = context;
	}

	DisplayManager dm = (DisplayManager) myContext.getSystemService(Context.DISPLAY_SERVICE);
	if (dm != null) {
		Display targetDisplay = dm.getDisplay(displayId);
		if (targetDisplay != null) {
			myContext = myContext.createDisplayContext(targetDisplay);
		}
	}
	return myContext;
}

Context getContext(boolean useAccessibilityService, int displayId) {
	if (useAccessibilityService) {
		return getContext(WindowManager.LayoutParams.TYPE_ACCESSIBILITY_OVERLAY, displayId);
	} else {
		return getContext(0, displayId);
	}
}

Context getContext(int overlayType) {
	int displayId = A11yConfig == void ? 0 : A11yConfig.displayId;
	return getContext(overlayType, displayId);
}

getContext(boolean useAccessibilityService) {
	int displayId = A11yConfig == void ? 0 : A11yConfig.displayId;
	return getContext(useAccessibilityService, displayId);
}

Context getContext() {
	int displayId = A11yConfig == void ? 0 : A11yConfig.displayId;
	return getContext(0, displayId);
}




