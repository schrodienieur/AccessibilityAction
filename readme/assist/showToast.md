# [showToast](/code/assist/showToast.bsh)

Show a short toast message on the device using the host UI thread.

&nbsp;
# Dependencies
None

&nbsp;
# How it works
`showToast` is a helper function or object from the assist folder that provides show a short toast message on the device using the host ui thread.
It exposes methods such as `showToast`.

# How to use

```java
showToast("Hello World");
```

## Return Value
Returns void.
